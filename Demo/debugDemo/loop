#include <stdio.h>

int max(int a, int b){
	return a > b?a:b ; 
}

int min(int a,int b){
	return a < b?a:b ; 
}

int main(){
	int a = 3 , b = 5 ; 
	int c = min ( a , b ) ; 
	int d = max ( a , b ) ; 
	int i ; 
	for ( i = c ; i <= d ; i++)
		printf ( "%d\n" , i ) ; 
		
	i ++ ; 
	i ++ ; 
	i += 2 ; 
	printf ( "%d" , i ) ; 
	return 0 ; 
}
